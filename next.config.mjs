// next.config.mjs
export default {
    reactStrictMode: true,  // Enables React strict mode for development (optional but recommended)
    images: {
      domains: ['admin.phinixx.com'],  // Add the external domain where your images are hosted
    },
  };
  